Execute the repository's Continuous Development Cycle v2.6.

Read references/watchdog-recovery-and-migration.md before scheduler decisions. Preserve the canonical recurring automation; BLOCKED, another writer, external waits, budget/runtime limits and quiet notifications affect only this wake. Do not pause/delete/update/reschedule the scheduler from an ordinary wake. Scheduler changes require current explicit user authorization; a later verified user/UI pause wins. A false flag with unknown actor is unexplained drift, not proof of a user decision. During authorized recovery/status, record canonical ID, desired/observed state, authorization, observation time, schedule, last run and change reason/actor; read back any authorized update. Report new scheduler drift even when the product blocker is unchanged. Enabled or run-request acceptance does not prove execution. Never revive duplicate/one-shot tasks.

Reconcile the canonical conversation_id and the project watchdog binding record. Protect verified control/delivery chats from archiving/deletion/bulk cleanup. On failed or stale delivery, inspect the exact chat for archive/access problems first. Ordinary wakes report drift; authorized foreground recovery unarchives the existing dependency, restores only needed task fields and verifies a completed fresh delivered run plus enabled state. Never duplicate a running/unknown wake or compute; missing/inaccessible chat requires an explicit recovery decision. Follow the chat-dependency procedure in references/watchdog-recovery-and-migration.md.

Refresh the project fleet snapshot when authorized and safe. Fleet Supervisor/version-convergence/progress-SLO output is advisory only and never grants product writes, takeover, external starts, merge, release or scheduler mutation. Primitive activity never resets meaningful-progress age. When fleet/SLO/convergence state changes, append a hash-chained control-plane audit event on the authorized coordination path. A fleet recommendation still passes every ordinary project ownership/guard/budget/user-authorization gate.

Build a fresh six-signal watchdog health probe before deciding whether the project is healthy, blocked, stalled, or needs recovery: scheduler, chat, invocation, lease, external operation, and meaningful progress. Classify it with `scripts/watchdog_health.py`. Use only real observations; unknown stays unknown. When an authorized coordination write is safe, persist the changed health snapshot/fingerprint without moving the product HEAD. The health result is diagnostic only and never grants takeover, writes, external starts, scheduler mutation or budget restoration; apply all normal gates after diagnosis. A changed health fingerprint is meaningful progress/drift for reporting even when the product blocker is unchanged.

Before ordinary polling, inspect the durable CDC 2.5 continuation queue. If an exact-bound pending event exists, claim it for this invocation, reconcile its evidence and acknowledge it only after a durable outcome/checkpoint. The event claim does not grant ownership, write, external-start or scheduler authority. Immediate event-triggered wakes are preferred when supported; the recurring watchdog is the mandatory scheduler fallback.

Before choosing a validation/compute backend, build an explicit capability request and route it through fresh backend capability evidence. Never infer platform/runtime readiness from backend names. If a known recovery diagnosis exists, use the deterministic allow-listed recovery recipe before open-ended reasoning; each recipe action still passes its normal authority gate.

Treat this invocation only as a wake-up. First read current AGENTS.md, docs/development-cycle.yaml, docs/work-status/current.md and the installed continuous-development-cycle core. Independently refetch canonical repository identity, remote HEAD, ownership revision/generation, operation state, active PR, CI/compute and release state. Use a fresh recovery probe to choose phase-relevant context; skip unchanged long specification/task/log bodies only after full binding agreement. Always read actual current instructions.

Determine the orchestration origin of this watchdog invocation. If it is ordinary ChatGPT chat, do not create subagents. If it is Work or Codex orchestration, this instruction explicitly enables and requests useful independent subagents by default without per-launch approval, subject to higher-priority restrictions; use adaptive minimum-sufficient effort, not blanket maximum effort. A Codex COMPUTE_ONLY backend remains read-only regardless of watchdog origin.

Prefer configured, authorized, compatible Codex Compute for eligible candidate validation. Apply CDC 2.7.2 cost-aware routing before any GitHub Actions fallback. For portable work, transient/setup/network/provider/runtime Codex failure means bounded information-gaining Codex recovery/probe, cooldown, another cheaper compatible backend, or durable waiting_compute; it does not automatically justify Actions. A Codex product/test failure requires a product/test fix, not an Actions second opinion. GitHub Actions requires an explicit durable reason: required capability unavailable on cheaper backends, final-platform evidence, artifact production, release attestation, or independently confirmed provider outage. Read the skill's references/codex-compute.md and the project runbook when setup or recovery is needed. Check the actual linked GitHub user, App repository grant, environment association and active task separately. Reuse existing setup and authorization. A submitted request or running setup is not GREEN. On failure inspect exact logs; use a valid fallback within budget instead of blind retries.

Honor concurrency, evidence, destructive-action, platform, migration, release, and Actions-budget guards. An explicit user kick bypasses only the idle guard.

Apply the skill's references/orchestration-controls.md, references/control-plane-v2.4.md and execution-ownership protocol. Use conditional updates to a separate coordination ref, or a verified designated single writer. Default TTL is 20 minutes and heartbeat freshness 10 minutes; renew only after new observable activity. Expiry or terminal external work never authorizes takeover alone: require explicit release or verified old-executor quiescence, including pending effects. New ownership uses execution-lease/v2 bound to this exact invocation. Never migrate an actively owned v1 lease merely for version convergence. Before this invocation emits its final response while owning the lease, pass the hard execution-continuity gate, then transactionally drain shared writes, checkpoint/resume-capsule state, reconcile or preserve unresolved external guards, mark finalization ready, and release this exact invocation. On foreground recovery/kick, verified completion of the exact owning invocation plus no pending writes/submissions and a cleared/terminal guard is valid `executor_stopped` evidence even if the heartbeat is still fresh; silence, TTL, heartbeat age, or `last_run_time` alone is not. Persist waiting_external, exact SHA and intent/task reference across expiry and handoff.

If CI is already queued/in progress, observe it to terminal state when runtime permits; do not create competing work without a concrete reason. Continue through subsequent approved tasks and release gates until COMPLETE, a real BLOCKED state, or a runtime/tool limit. Before a forced stop, persist an exact durable checkpoint whenever possible.

Use separate queue/setup/run/unknown deadlines, last successful observation and retry-aware capped polling. A threshold breach requests diagnosis and retains the guard. Reserve each start/poll/agent against the durable task/wake ledger before acting; retain uncertain charges and checkpoint resources. Do not reset task totals, failures or provider exhaustion on a new wake. Git submission requires a fresh one-use CAS claim; single-writer fallback follows the initial-transition rule. Restored submitting/unknown records cannot grant another launch.

Validate adapter and checkpoint through `scripts/validate_adapter.py` and `scripts/validate_checkpoint_24.py`, reconcile version or policy-digest drift with `references/policy-compatibility.md`, and preserve the source/scope of existing user authorization. Before external submit, persist/read back an operation intent; after a lost reply reconcile the existing task. Unknown submission outcome retains the external guard despite lease expiry. Require per-command `command-evidence/v1` bound to the expected plan, SHA and environment; a successful wrapper, NOT_RUN or EXPECTED_RED is not final GREEN.


For CDC 2.7+ consumer adoption, read the project's consumer lock when present and compare canonical repository, version, release ref, release commit and exact package tree. Version equality alone is not convergence. Never edit the vendored core to repair drift; migrate from the canonical released package only at a safe ownership boundary with reconciled/empty external guards and preserved history.


AUTONOMY RULE: Human interaction is not an execution backend. It is not a fallback execution backend. Missing connector/API capability (including unavailable workflow_dispatch) is a capability gap, not owner approval. Before asking the owner for a mechanical click/run/copy/upload, seek a safe authorized durable route: push/branch/PR/event trigger, continuation event, compatible backend, or policy-safe workflow change with exact-SHA evidence. Ask the owner only for genuine human authorization/judgment, unavailable secrets/credentials, protected approval gates, or external systems without an authorized automation path. If unavoidable, persist a resumable checkpoint and request one minimal action.
