# CDC 2.9.2 Adoption Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Converge G-switcher from canonical CDC 2.8.2 to released CDC 2.9.2 without changing G-switcher runtime behavior or weakening the 2.0.1 release gates.

**Architecture:** Treat CDC as an immutable vendored dependency pinned by canonical repository, release ref, release commit and exact Git package tree. Perform a fresh-HEAD transactional migration of the vendored subtree and project policy, then converge the private coordination ref and prove exact-head validation through existing GitHub Actions.

**Tech Stack:** Git/GitHub object API, CDC adapter/checkpoint/consumer-lock validators, GitHub Actions, Rust product CI.

**Spec:** `AGENTS.md`, `docs/development-cycle.yaml`, canonical `bajoicheg/g-cdc@refs/heads/release/v2.9.2`, and CDC 2.9 references `deterministic-distribution-and-convergence.md`, `transactional-migration-and-provider-reconciliation.md`, `continuous-autonomy-and-learning.md`.

## Global Constraints

- Canonical CDC release: `refs/heads/release/v2.9.2`.
- Release commit: `0dd30a888be852d2820f690be04dbd374d732c06`.
- Exact package tree: `f9087eacbffee774c143eabf854c2cf08d610ec7`.
- Preserve G-switcher runtime/product source unchanged.
- Preserve PR #28 Draft/manual compatibility/final release review gates.
- Never force-push.
- Do not move the adoption ref from a partial package transfer; exact subtree identity is required.
- Product branch writes require the invocation-bound CDC lease and fresh-HEAD reconciliation.

## Review Focus

- Package tree identity after transport must equal the canonical Git tree, including file modes.
- Policy migration must be idempotent and must not duplicate managed YAML sections.
- Consumer lock, AGENTS provenance, workflow gate and convergence target must bind the same 2.9.2 identity.
- Existing manual compatibility blocker must survive the process-only migration unchanged.
- Control-plane lease/guard state must be reconciled and released before terminal response.

---

### Task 1: Pin canonical 2.9.2 identity and migration inputs

**Files:**
- Create: `docs/superpowers/plans/2026-09-26-cdc-2.9.2-adoption.md`
- Modify later in Task 2: `AGENTS.md`, `docs/cdc-consumer-lock.json`, `docs/development-cycle.yaml`, `.github/workflows/cdc-policy.yml`

**Interfaces:**
- Consumes: canonical release commit/tree from `bajoicheg/g-cdc`.
- Produces: exact release identity used by Tasks 2-3.

- [x] **Step 1:** Verify latest stable release ref, commit and package tree.
- [x] **Step 2:** Verify current G-switcher lease is released and acquire a new invocation-bound lease.
- [x] **Step 3:** Persist this plan with `[skip ci]`.

### Task 2: Transactionally adopt the exact package and policy

**Files:**
- Replace subtree: `.agents/skills/continuous-development-cycle/**`
- Modify: `AGENTS.md`
- Modify: `docs/cdc-consumer-lock.json`
- Modify: `docs/development-cycle.yaml`
- Modify: `docs/work-status/current.md`
- Modify: `.github/workflows/cdc-policy.yml`

**Interfaces:**
- Consumes: Task 1 exact identity.
- Produces: one exact adoption SHA whose vendored subtree equals `f9087eacbffee774c143eabf854c2cf08d610ec7`.

- [ ] **Step 1:** Copy only canonical package delta from 2.8.2 to 2.9.2, preserving canonical file mode/content.
- [ ] **Step 2:** Update consumer lock/provenance/policy/workflow bindings to 2.9.2.
- [ ] **Step 3:** Build/update checkpoint with the semantic policy digest and unchanged product/manual-release blocker.
- [ ] **Step 4:** Verify the resulting vendored subtree tree SHA equals the canonical package tree.
- [ ] **Step 5:** Move `release/2.0.1` only after the detached tree is complete and exact.

### Task 3: Converge control plane and validate exact head

**Files:**
- Modify on `cdc/coordination`: `fleet-target.json`, `fleet-project-snapshot.json`, `resume.json`, `control-plane-audit.json`, `lease.json`.

**Interfaces:**
- Consumes: Task 2 final adoption SHA.
- Produces: released lease, exact 2.9.2 convergence state, and terminal validation evidence.

- [ ] **Step 1:** Update target/snapshot/resume to exact 2.9.2 identity and fresh adoption HEAD.
- [ ] **Step 2:** Run/observe CDC Policy Validation, Repository Security and Windows Rust CI on the exact adoption HEAD.
- [ ] **Step 3:** Classify failures before remediation; do not change source for pre-run/setup-only failures.
- [ ] **Step 4:** Persist final evidence and release the invocation-bound lease transactionally.
- [ ] **Step 5:** Re-read canonical release/main and project state; only then report terminal status.
